server:
  port: ${PORT:7071}
eureka:
  instance:
    hostname: eureka-server
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://10.71.171.22}:${EUREKA_SERVER_PORT:7071}/eureka/
  server:
    enable-self-preservation: true
---
spring:
  profiles: stage-eureka1
eureka:
  instance:
    hostname: stage-eureka1
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://10.71.174.22}:${EUREKA_SERVER_PORT:7071}/eureka/
  server:
    enable-self-preservation: true
---
spring:
  profiles: stage-eureka2
eureka:
  instance:
    hostname: stage-eureka1
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://10.71.172.22}:${EUREKA_SERVER_PORT:7071}/eureka/
  server:
    enable-self-preservation: true
